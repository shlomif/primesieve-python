language: python

python:
  - 3.6
  - 3.7

jobs:
  include:
    # perform a linux build
    - services: docker
    # and a mac build
    - os: osx
      language: shell
    # and a windows build
    - os: windows
      language: shell
      before_install:
        # choco requires Python version format: Major.Minor.Patch
        - export CHOCO_PYTHON_VERSION="$TRAVIS_PYTHON_VERSION.0"
        - export PYTHON_VERSION_WITHOUT_DOT="${TRAVIS_PYTHON_VERSION//.}"
        - choco install python --version $CHOCO_PYTHON_VERSION
        - export PATH="/c/Python$PYTHON_VERSION_WITHOUT_DOT:/c/Python$PYTHON_VERSION_WITHOUT_DOT/Scripts:$PATH"

env:
  global:
    - TWINE_USERNAME=kimwalisch
    # Note: TWINE_PASSWORD is set in Travis settings

install:
  - python -m pip install -U pip setuptools
  - python -m pip install -U cython numpy
  - python -m pip install -U pytest
  - python -m pip install -U twine
  - python -m pip install -U cibuildwheel

script:
  - git submodule update --init
  - python setup.py install
  - py.test
  - export PYTHON_VERSION_WITHOUT_DOT="${TRAVIS_PYTHON_VERSION//.}"
  - export CIBW_BUILD="cp$PYTHON_VERSION_WITHOUT_DOT-*"
  - export CIBW_BEFORE_BUILD="python -m pip install -U cython numpy"
  - python -m cibuildwheel --output-dir wheelhouse .

after_success:
  - python -m twine upload --skip-existing --verbose --repository-url https://test.pypi.org/legacy/ wheelhouse/*.whl
  - if [[ $TRAVIS_TAG ]]; python -m twine upload --skip-existing --verbose wheelhouse/*.whl;  fi
